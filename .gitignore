# 字节码和编译文件
__pycache__/
*.py[cod]
*$py.class
*.pyc

# Python 虚拟环境
# 这条规则非常重要，可以防止你把整个虚拟环境提交上去
venv/
env/
.venv/
.env/
*/venv/
*/env/
*/.venv/
*/.env/

# IDE 和编辑器配置文件
.vscode/
.idea/
*.swp
*.swo

# 日志和数据文件
*.log
*.log.*
*.csv
*.zip
*.pth  # 常见的模型权重文件，通常很大
*.ckpt # 常见的模型检查点文件

# 存放本地实验结果或数据的文件夹 (自定义)
# 比如你创建了一个 outputs/ 文件夹来存放生成的图片，你可能不想提交它
# 希望 .gitignore 只忽略项目根目录下的 datasets 文件夹及其所有内容，而保留所有子目录中的 datasets 文件夹,注意git的模式匹配规则，可以问gemini
/outputs/  
/datasets/
/checkpoints/
/deps/
/results/
/experiments/

# 非本分支的文件
AddingCodes/

# 操作系统生成的文件
.DS_Store
Thumbs.db

# 忽略所有 PyTorch 模型权重文件，在任何目录层级
# 排除 style_translator_dataset.pt
*.pt

# 忽略所有顶层以 'checkpoints/' 结尾的目录
# 排除 translator_checkpoints/ 等所有检查点目录
*/checkpoints/